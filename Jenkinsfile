pipeline {
    agent any

    stages {
        stage('prepare environment'){
            steps {
                echo ${BUILD_NUMBER}
            }
        }
        stage('build'){
            steps {
                echo "Test"
            }
        }
    }
}